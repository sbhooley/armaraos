//! Backward-compatible prompt compression re-export.
//!
//! The implementation now lives in `ainl-compression` so other hosts can reuse
//! the same compression logic without depending on `openfang-runtime`.

pub use ainl_compression::*;
